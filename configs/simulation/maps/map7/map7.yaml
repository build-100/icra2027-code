image: map7.png
# Should be consistant with .world file
resolution: 0.1
# Lower-left pixel in the centered static map frame, independent of robot pose.
# 138.2 m by 66.4 m; the old origin included a single robot's [-55,-20] offset.
origin: [-69.1, -33.2, 0]
occupied_thresh: 0.65
free_thresh: 0.196
negate: 0
